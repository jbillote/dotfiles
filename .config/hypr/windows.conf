##############################
### WINDOWS AND WORKSPACES ###
##############################

windowrule {
    name = supress_maximize
    suppress_event = maximize
    match:class = .*
}

windowrule {
    name = discordWorkspace
    match:class = discord.*
    workspace = 2
    no_initial_focus = on
}

windowrule {
    name = chromiumWorkspace
    match:class = chromium.*
    workspace = 2
    no_initial_focus = on
}

# Games
windowrule {
    name = umamusumeFloat
    match:class = steam_app_3224770
    float = on
}

windowrule {
    name = hsrWorkspace
    match:class = starrail.exe
    workspace = 1
    no_initial_focus = off
}

windowrule {
    name = btopWorkspace
    match:class = jbillote.btop
    workspace = 10
    no_initial_focus = on
}

# Fix some dragging issues with XWayland
windowrule {
    name = xwaylandDrag
    no_focus = on
    match:class = ^$,
    match:title = ^$,
    match:xwayland = 1
    match:float = 1
    match:fullscreen = 0
    match:pin = 0
}
